# Server.list_tools()/call_tool() decorator API used by argus/mcp_server/server.py.
# Newer incompatible releases can otherwise make a freshly built `latest`
# image crash even when application tests pass against an older local venv.
mcp==1.28.1
starlette
uvicorn
python-dotenv

pydantic
pydantic-settings
structlog
cachetools
httpx
PyYAML

aioca
p4p
kubernetes
elasticsearch[async]
scikit-learn
